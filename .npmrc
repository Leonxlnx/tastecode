# Keep the dependency tree honest — no phantom deps that only resolve by accident.
# This bites hardest on Windows, where hoisting differs from CI on macOS.
strict-peer-dependencies=false
auto-install-peers=true
resolution-mode=highest

# Native modules (better-sqlite3, node-pty) need to run their build scripts.
enable-pre-post-scripts=true
